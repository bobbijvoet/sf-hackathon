/*
'use strict';



class ExampleController {
  constructor(ExampleService) {
    this.ExampleService = ExampleService;
    this.init();
  }

  init() {
    this.ExampleService.getStarred().then(users => {

      this.users = users;
    });
  }
}

ExampleController.$inject = ['ExampleService'];

export default ExampleController;*/
